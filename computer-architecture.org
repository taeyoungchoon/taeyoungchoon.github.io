* list and atom

- cpu, mem, dsk
  - net
  - sec
- cpu, mem
  - dsk
  - net
  - sec
- processor와 memory가 기본 뼈대를 이룬다. memory에 program를 적고, processor에 읽어들여서 처리한다. 연산을 수행한다.
  - 메모리에는 주소가 있고, 워드 단위로 읽는다.
    - 캐쉬와 가상메모리가 있다.
      - 가상메모리는 메모리의 덩치를 키운다.
      - 캐쉬는 빠르다.
- dsk, net, sec.
  - 디스크는 메모리에 가기 전의 저장소이다.
  - 네트워크는 통신을 가능하게 한다. 하나에서 둘로, 셋으로.
  - 보안은 독자적인 영역에서 내려온다는 것이다. 이제는 함께 한다.
    - 개인정보보호법

* 글이라, 정리가 되면 글이 되는가? 아직 되지 못했다. 글이라. 꼭, 그런 이야기는
